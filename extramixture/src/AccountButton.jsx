import React from "react";
import reactDOM from "react-dom/client";
import Account from "./Account";
reactDOM.createRoot(document.getElementsByClassName("profile")[0]).render(
    <React.StrictMode>
        <Account/>
    </React.StrictMode>
);